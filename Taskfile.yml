version: "3"
# see: https://taskfile.dev/usage/

tasks:
  build:
    cmds:
      - go build
  release-build:
    cmds:
      - goreleaser --snapshot --clean
  fmt:
    cmds:
      - goimports -w main.go

